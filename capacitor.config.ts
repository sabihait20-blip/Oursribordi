import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amadersreebordi.app',
  appName: 'আমাদের শ্রীবরদী',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
